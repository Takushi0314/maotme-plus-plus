import { Box } from '@mui/material';

export default function Homepage() {
  return (
    <main>
      <section>
        <Box sx={{ textAlign: 'center' }}>サンプルセクション</Box>
      </section>
    </main>
  );
}
